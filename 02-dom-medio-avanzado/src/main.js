import divForm from "./components/divForm.js";


document.addEventListener("DOMContentLoaded", function() {
    const app = document.getElementById("app");

    // EJERCICIO CONTENEDOR FORMULARIO TEST EMAIL
    // const divFormulario = divForm();
    // app.appendChild(divFormulario); // <-- Contenedor dentro de app


    // CREAR UNA TABLA DINÁMICAMENTE
    

});


// HACER SING UP -> username, 2 pass, label (pass no coinciden)